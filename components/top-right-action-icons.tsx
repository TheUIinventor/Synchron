"use client";

// Mobile top-right action menu removed per UX request.
// This component intentionally returns null so the old hamburger menu
// is no longer rendered. Theme toggle has been moved to Appearance settings.
export default function TopRightActionIcons() {
  return null;
}
